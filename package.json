{
  "private": true,
  "scripts": {
    "build": "exec-bin bin/hugo/hugo --minify",
    "create": "exec-bin bin/hugo/hugo new",
    "dev": "exec-bin bin/hugo/hugo server --bind=0.0.0.0",
    "postinstall": "hugo-installer --version 0.123.4 --extended"
  },
  "devDependencies": {
    "exec-bin": "^1.0.0",
    "hugo-installer": "^4.0.1"
  }
}
